# NUCLEAR OPTION: Ignore EVERYTHING except what we explicitly need

# First, ignore EVERYTHING at root
/*

# Then selectively allow ONLY what's needed
!vercel.json
!apps/
apps/*
!apps/web/

# Within apps/web, ignore build artifacts and local configs
apps/web/.next/
apps/web/node_modules/
apps/web/.turbo/
apps/web/.env.local
apps/web/.env.*.local

# Ignore any workspace-related files that might have sneaked in
**/*workspace*
**/pnpm-lock.yaml
**/.npmrc

# Ignore TypeScript build info
**/*.tsbuildinfo

# Ignore test coverage
**/coverage/